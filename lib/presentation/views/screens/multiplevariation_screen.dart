import 'package:flutter/material.dart';

class MultiplevariationScreen extends StatelessWidget {
  const MultiplevariationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Single Variation"),
      ),
    );
}
}