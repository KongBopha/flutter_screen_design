import 'package:flutter/material.dart';

class SinglevariationScreen extends StatelessWidget {
  const SinglevariationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Single Variation"),
      ),
    );
  }
}